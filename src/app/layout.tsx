import type { Metadata } from "next";
import "./globals.css";
import { SiteHeader } from "@/components/site-header";
import { SiteFooter } from "@/components/site-footer";

export const metadata: Metadata = {
  title: {
    default: "Contractor Invoice Guide — Honest invoicing software comparisons for tradespeople",
    template: "%s | Contractor Invoice Guide",
  },
  description:
    "Independent, hands-on comparisons of invoicing and accounting software built for contractors, electricians, plumbers, and other trades.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="flex min-h-screen flex-col bg-background text-foreground antialiased">
        <SiteHeader />
        <div className="flex-1">{children}</div>
        <SiteFooter />
      </body>
    </html>
  );
}
